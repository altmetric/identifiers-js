/**
 * Extract National Clinical Trial (NCT) identifiers from a string.
 * Results are normalized to uppercase.
 *
 * @param str - The string to search for NCT identifiers
 * @returns An array of NCT identifiers found in the string
 */
export function extract(str: string): string[];
