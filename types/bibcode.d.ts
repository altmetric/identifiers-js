/**
 * Extract ADS Bibcode identifiers from a string.
 *
 * @param str - The string to search for Bibcode identifiers
 * @returns An array of Bibcode identifiers found in the string
 */
export function extract(str: string): string[];
