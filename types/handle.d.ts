/**
 * Extract Handle identifiers from a string.
 *
 * @param str - The string to search for Handle identifiers
 * @returns An array of Handle identifiers found in the string
 */
export function extract(str: string): string[];
